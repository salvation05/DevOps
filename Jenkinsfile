pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {
                    def name = "Piyush"
                    if (name == "Piyush")
                      println("Hi ${name}")
                    else 
                      println("Hello World")
                }
            }
        }
    }
}
